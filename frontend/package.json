{
  "name": "stock-simulation-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "build:deploy": "npm run deploy",
    "deploy": "npm run build && npx gh-pages -d dist -b gh-pages -r https://github.com/Dongyeon-k/stock_simulation_game.git",
    "deploy:ssh": "npm run build && npx gh-pages -d dist -b gh-pages -r git@github.com:Dongyeon-k/stock_simulation_game.git",
    "deploy:preview": "npm run build && npx gh-pages -d dist -b gh-pages"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
